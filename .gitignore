node_modules/
*.tmp
# Sprint 15: respaldos efímeros de la promoción a staging (regenerables)
api/v1/staging/.rollback/
# Sprint 17: sign-off humano de promoción a producción (NO versionar, sin secretos)
.promotion-signoff.json
# Sprint 18: segunda confirmación de release (NO versionar, sin secretos)
.release-confirmation.json
# Sprint 18: respaldos efímeros de canónicos y salidas de simulacro (regenerables)
.canonical-rollback/
.release-sim/
# Sprint 19: screenshots del QA visual opt-in y salidas del RC (regenerables)
.rc-qa/
.rc-package/
# Sprint 20: sign-off editorial real (NO versionar, sin secretos; el ejemplo .editorial-signoff.example.json sí se versiona)
.editorial-signoff.json
# Sprint 22: decisiones editoriales humanas reales (NO versionar, sin secretos; el ejemplo .editorial-decisions.example.json sí se versiona)
.editorial-decisions.json
# Sprint 23: registro de claves PÚBLICAS reales de firma (NO versionar; el ejemplo editorial-signature-keys.example.json sí se versiona). NUNCA versionar claves privadas.
editorial-signature-keys.json
# Sprint 23: material de firma PRIVADO local (NO versionar jamás)
*.ed25519.key
*.private.pem
.editorial-signatures/
