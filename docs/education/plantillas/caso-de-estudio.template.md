# Plantilla: Caso de estudio GEOPÓLEM

> Plantilla reutilizable. Usa un ID de conflicto **existente** del inventario y
> datos RC/staging o v1 verificado. **No inventes hechos.** Material de
> formación: no sustituye la revisión editorial final ni activa producción.

## Identificación del conflicto

- **conflict_id:** `<id existente en data/conflicts.inventory.json>`
- **name:** `<name>`
- **conflict_type:** `<slug — label>`
- **primary_region:** `<slug — label>`
- **status:** `<active | ...>`
- **Fase de datos:** `<rc | staging | v1-verified>`

## Contexto

`<Resumen del foco basado en summary y en fuentes verificadas.>`

## Actores

- **Estatales (`actors.state`):** `<...>`
- **No estatales (`actors.non_state`):** `<...>`

## Recursos y chokepoints

- **Recursos (`resources`):** `<...>`
- **Chokepoints (`chokepoints`):** `<nombre — energy_flow_relevance>`

## Cadena causal

| Enlace (`link_type`) | Título | Explicación | Fuente de soporte |
|---|---|---|---|
| `<causes/escalates/...>` | `<title>` | `<explanation>` | `<source_slug>` |

## Fuentes

| Slug | Título | Editor | URL | accessed_at | verification |
|---|---|---|---|---|---|
| `<slug>` | `<title>` | `<publisher>` | `<url>` | `<fecha>` | `<verified>` |

## Preguntas de análisis

1. `<pregunta 1>`
2. `<pregunta 2>`
3. `<pregunta 3>`

## Advertencia editorial

Este caso es **material docente** basado en datos RC/staging. No sustituye la
revisión editorial final ni implica publicación/aprobación de contenido. La
producción permanece bloqueada por política.
